#!/usr/bin/python3
"""Module that defines a Square class."""


class Square:
    """Class representing a square."""

    def __init__(self, size):
        """Initialize a new Square instance.

        Args:
            Size (init): The size (side length) of the square."""

        self.__size = size
